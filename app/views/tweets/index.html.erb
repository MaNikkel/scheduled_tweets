<h1>Tweets</h1>
<% if Current.user.twitter_accounts.any? %>
  <%= link_to "Schedule a Tweet", new_tweet_path %>
<% else %>
  <%= button_to "Connect Twitter", "/auth/twitter", method: :post, class: "btn btn-primary"%>
<% end %>

<%= render @tweets%>